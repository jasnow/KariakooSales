<%= simple_form_for @quality, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :value %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                qualities_path, :class => 'btn' %>
  </div>
<% end %>
